import QtQuick

import "../../Config"

Text {
    lineHeight: 21.6
    lineHeightMode: Text.FixedHeight

    color: CC.colorText

    font {
        family: TC.fontFamily
        weight: 400
        pixelSize: 16
    }

    wrapMode: Text.Wrap
}
