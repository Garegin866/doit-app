import QtQuick

import "../../Config"

Text {
    lineHeight: 16
    lineHeightMode: Text.FixedHeight

    color: "#0E0E11"

    font {
        family: TC.fontFamily
        weight: 400
        pixelSize: 13
        letterSpacing: 0.02
    }

    wrapMode: Text.Wrap
}
